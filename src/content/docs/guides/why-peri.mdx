---
title: 为什么选择 Peri
description: Peri vs Claude Code vs Cursor——技术对比、架构差异与性能数据。
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

## 不是"又一个" AI 工具

Peri 不是 Claude Code 的替代品，而是一个**兼容生态的 Rust 重实现**。它做的是同一件事——AI 驱动的编码助手——但做得更轻、更快、更开放。

## 核心对比

| 维度 | Peri | Claude Code | Cursor / Copilot |
|------|------|-------------|------------------|
| **语言** | Rust | TypeScript / Node.js | TypeScript / Node.js |
| **二进制大小** | ~13 MB | ~80 MB (含 Node 运行时) | N/A (IDE 插件) |
| **运行时内存** | ~50 MB | ~300-800 MB | ~500-1500 MB |
| **启动时间** | `「0.5s」` | 2-5s | IDE 启动时间 |
| **缓存命中率** | 95-99% | ~60-80% | 取决于实现 |
| **LLM 支持** | 任意模型，会话中切换 | 主要 Claude，有限 OpenAI | 主要 Claude / GPT |
| **配置兼容** | 完全兼容 CC config | 原生 | 不兼容 |
| **开源协议** | Apache 2.0 | 专有 | 专有 |

## 为什么 Rust？

<Tabs syncKey="why-rust">
  <TabItem label="内存">

  Node.js 的 V8 引擎在运行时占用大量内存，且不受开发者控制。Peri 不需要托管运行时——**一个 13MB 的静态链接二进制文件**，启动即用，不会膨胀。

  </TabItem>
  <TabItem label="缓存">

  缓存是 AI 编码工具中**最被低估的性能杠杆**。Peri 的冻结系统提示词 + 边界标记机制，让 95-99% 的对话 token 直接从缓存命中——你几乎不为重复的上下文付费。

  ```text
  传统方式: 每次对话重新发送全部上下文 → 100% 新 token
  Peri 方式:  冻结前缀 + 增量更新         → 1-5% 新 token
  ```

  </TabItem>
  <TabItem label="并发">

  Rust 的 async 运行时（Tokio）天然支持高并发。Peri 可以同时运行**多个后台子代理**（编码、探索、代码审查、Web 研究），而 Node.js 的单线程事件循环难以优雅地处理这种负载。

  </TabItem>
</Tabs>

## 缓存机制详解

<Aside type="tip" title="这是 Peri 最核心的竞争优势">
  如果你只用一条理由说服团队使用 Peri，选这条——它直接影响 API 费用。
</Aside>

Peri 使用**确定性冻结前缀**（deterministic frozen prefix）策略：

1. 系统提示词在编译时被序列化为固定字节序列
2. 边界标记（boundary marker）定义了可缓存 / 不可缓存的切分点
3. 每个请求复用已缓存的提示词前缀，只发送增量的用户/助手消息

这意味着：
- 第 1 次对话：发送完整系统提示词 → 建立缓存
- 第 2+ 次对话：**只发送增量的对话内容**，前缀从缓存命中

对于重度用户（每天数百次对话），缓存命中带来的节省可以降低 **90% 以上的 token 消耗**。

## 模型自由

<Aside type="caution" title="Claude Code 的限制">
  Claude Code 设计上是为 Anthropic API 优化的。虽然有社区方案接入其他模型，但体验和性能都不理想。
</Aside>

Peri 的 LLM 适配层是**模型无关的**。所有模型通过统一的 trait 接口接入：

```rust
// Peri 的 LLM 适配层（简化示意）
#[async_trait]
pub trait LlmProvider {
    async fn chat(&self, req: ChatRequest) -> Result<ChatResponse>;
    async fn stream(&self, req: ChatRequest) -> Result<Stream>;
    fn supports_tools(&self) -> bool;
    fn context_window(&self) -> usize;
}
```

DeepSeek、GLM、Qwen、Anthropic、OpenAI——都实现了同一个 trait。切换模型只是换一个实现，不涉及任何业务逻辑改动。

<Aside type="note">
  Peri 支持**会话中切换模型**。比如你可以用 DeepSeek V3 做日常编码，遇到难题时 `/model claude-sonnet` 切换到更强的推理模型，几分钟后再切回来——无缝、无重启。
</Aside>

## AI 驱动的自我进化

Peri 自身就是 Peri 写的。代码库的 **99% 由 AI（DeepSeek 和 GLM-5.2）生成**。

开发流程是一个 AI 自我驱动的闭环：

| 当你…… | 闭环启动 |
|--------|---------|
| 发现 bug 或技术债务 | 创建 issue → 系统诊断 → 撰写计划 → 子代理开发 → 归档 issue → 更新知识库 |
| 想要新功能 | 需求访谈 → 撰写计划 → 子代理开发 |
| 代码库变乱 | 清理分析 → 架构优化 → 撰写计划 → 子代理开发 |

<Aside type="tip">
  每一个修复中发现的非显而易见约束都会被自动写回 `CLAUDE.md`，形成**TRAP**——一个硬性规则，在后续所有迭代中强制遵守。这些 TRAP 不是人类撰写的，而是 AI 在 bug 现场提取的。这就是**质量在无人类审查的情况下持续累积**的方式。
</Aside>
