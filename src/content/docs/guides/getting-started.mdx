---
title: 快速上手
description: 从零开始安装 Peri——一个 13MB 的 Rust 编码助手，兼容 Claude Code 生态，支持任何 LLM。
---

import { Aside, FileTree } from '@astrojs/starlight/components';

## 系统要求

Peri 提供 **macOS** (x86_64 / Apple Silicon)、**Linux** (x86_64 / aarch64 / riscv64) 和 **Windows** (x86_64) 的预编译二进制文件。

<Aside type="tip" title="无需 Node.js">
  Peri 用 Rust 编写，是单个静态链接的二进制文件。不需要安装 Node.js、Python 或任何运行时依赖。
</Aside>

## 安装

### 1. 下载安装脚本

打开终端，运行以下命令：

```bash
# macOS / Linux
curl -fsSL https://raw.githubusercontent.com/konghayao/peri/main/scripts/install.sh | bash
```

Windows 用户使用 PowerShell：

```powershell
irm https://raw.githubusercontent.com/konghayao/peri/main/scripts/install.ps1 | iex
```

### 2. 启动 Peri

```bash
peri
```

首次启动会进入交互式配置向导，引导你完成模型选择和 API Key 设置——**无需手动编辑任何配置文件**。

### 3. 更新到最新版

```bash
peri update
```

一条命令完成自我更新。

## 配置模型

Peri 支持**任何 OpenAI 兼容的 API**。以下模型已通过充分测试：

| 模型 | 提供商 | 推荐用途 |
|------|--------|----------|
| **Claude 4 Sonnet** | Anthropic | 复杂编码、架构设计 |
| **DeepSeek V3 / R1** | DeepSeek | 高性能、低成本的日常编码 |
| **GLM-5.x** | 智谱 | 中文场景最优 |
| **Qwen 3 Max** | 阿里 | 通用编码任务 |
| **GPT-4o / 4.1** | OpenAI | 全面推理 |

<Aside type="note">
  你可以在一次会话中**随时切换模型**，无需重启 Peri。只需用 `/model` 命令指定新模型即可。
</Aside>

## 与 Claude Code 的兼容性

如果你已经有 Claude Code 的配置（`.claude/settings.json`、skills、hooks、MCP 服务器等），**零迁移即可使用**。

Peri 完全兼容：

- **Skills** — `.claude/skills/` 目录下的所有 skill 直接可用
- **Hooks** — 会话生命周期钩子无需修改
- **MCP** — Model Context Protocol 插件即插即用
- **Sub-agents** — `.claude/agents/` 下的代理定义无缝工作

<Aside type="caution" title="注意">
  如果你同时安装了 Claude Code 和 Peri，它们各自拥有独立的会话存储，不会互相干扰。
</Aside>

## 验证安装

启动 Peri 后，你可以验证一切是否正常：

```bash
# 查看 Peri 版本
peri --version

# 查看当前配置
peri config show

# 测试一次简单对话
peri -p "用 Python 写一个快速排序" --max-turns 1
```

## 下一步

- 阅读 [为什么选择 Peri](/guides/why-peri/) 了解技术对比
- 查看 [架构设计](/reference/architecture/) 理解 Peri 的内部结构
- 阅读 [Nobody Coding](https://github.com/konghayao/peri/blob/main/docs/blogs/ai-coding-paradigm/nobody-coding.md) 了解 Peri 的 AI 驱动开发哲学
